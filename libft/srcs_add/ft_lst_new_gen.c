
#include "libft_bonus.h"

// a generic version of ft_lstnew_gen
/* 
void	*ft_lstnew_gen(void *content, size_t node_size)
{
	void	*new;

	new = (node_size *)malloc(sizeof(node_size));
	if (!new)
		return (NULL);
	new->content = content;
	new->next = NULL;
	return (new);
}
*/